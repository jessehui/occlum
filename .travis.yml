sudo: required

services:
  - docker

before_install:
- pwd
- cd ..
- docker run -v /home/travis/build/jessehui/occlum:/root/occlum occlum/occlum:latest /bin/bash -c "cd /root/occlum; make submodule; make; make test"

script:
- echo done
- sleep 3
